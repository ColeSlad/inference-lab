services:
  vllm:
    command:
      - --model
      - ${INFERENCE_LAB_MODEL:-Qwen/Qwen3-0.6B}
      - --revision
      - ${INFERENCE_LAB_MODEL_REVISION:-main}
      - --dtype
      - ${INFERENCE_LAB_MODEL_DTYPE:-auto}
      - --enable-prefix-caching
      - --gpu-memory-utilization
      - "0.90"
